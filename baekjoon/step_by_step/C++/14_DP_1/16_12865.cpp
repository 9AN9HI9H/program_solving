/**
 * https://www.acmicpc.net/problem/12865
 * 
 * BOJ 12865 - 평범한 배낭
 * 
 * 입력 : 물품의 수 N (1 <= N <= 100), 버틸 수 있는 무게 K (1 <= K <= 1e5),
 *        각 물건의 무게 W[1..N] (1 <= W[i] <= 1e5), 해당 물건의 가치 V[1..N] (1 <= V[i] <= 1e5)
 * 출력 : 배낭에 넣을 수 있는 물건들의 가치의 합의 최댓값
 **/

#include <iostream>
#include <vector>
using namespace std;
using pii = pair<int, int>;
using vi = vector<int>;
using vpii = vector<pii>;

void solve() {
    int n, k;

    cin >> n >> k;
    
    return;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}
